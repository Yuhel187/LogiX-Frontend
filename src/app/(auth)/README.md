# Authentication Route Group

Routes sharing the unauthenticated/authentication layout belong here. The route
group name does not affect the URL. Keep reusable identity behavior in
`src/features/identity`.
