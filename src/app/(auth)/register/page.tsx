import { RegisterScreen } from "@/features/identity";

export const metadata = {
  title: "LogiX Platform",
  description: "Trang đăng ký tài khoản hệ thống quản trị Logistics LogiX",
};

export default function RegisterPage() {
  return <RegisterScreen />;
}
