import { SetPasswordScreen } from "@/features/identity";

export const metadata = {
  title: "LogiX Platform",
  description: "Trang thiết lập mật khẩu hệ thống quản trị Logistics LogiX",
};

export default function ResetPasswordPage() {
  return <SetPasswordScreen />;
}
