import { LoginScreen } from "@/features/identity";

export const metadata = {
  title: "LogiX Platform",
  description: "Trang đăng nhập hệ thống quản trị Logistics LogiX",
};

export default function LoginPage() {
  return <LoginScreen />;
}
