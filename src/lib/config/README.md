# Frontend Configuration

Centralize validated environment and runtime configuration here. Server-only
values must not cross into client bundles; only explicitly public values may use
the `NEXT_PUBLIC_` prefix.
